import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.xcorat.flagtuk',
  appName: 'Flag Tuk',
  webDir: 'build',
  bundledWebRuntime: false,

  // server: {
  //   'url': 'localhost:3000',
  //   'cleartext': true,
  // }
};

export default config;
